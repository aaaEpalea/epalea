# Authentication

All API requests require an `X-API-Key` header.

```bash
curl https://epalea.ai/api/v1/models \
  -H "X-API-Key: ek_live_xxxxxxxxxxxxxxxxxxxx"
```

API keys are available at `epalea.ai/account/keys` (coming soon).
During the beta, email team@epalea.ai to request access.
